-if cart_empty?
  .col-lg-6.col-md-6.col-sm-6.col-lg-offset-4.col-md-offset-4.col-sm-offset-4
    %h3 #{t('empty_cart')}
-else
  = render partial: 'items', locals: {order: @current_order}
  -#= render 'add_coupon'
